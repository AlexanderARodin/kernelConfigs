#!/bin/sh
set -e

while IFS= read -r LINE; do
	FIRST=$(echo "$LINE" | cut -c 1)
	if [ '#' = $FIRST ]; then
		echo "IGNORED: $LINE"
	else
		echo "$FIRST-line: $LINE"
		for ITEM in $LINE; do
			echo "item: $ITEM"
		done
	fi
done < $1
